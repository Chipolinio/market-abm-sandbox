/** Root shell — dashboard components land in slice 7.3. */
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "1.5rem" }}>
      <h1>Market ABM Dashboard</h1>
      <p>Slice 7.2 — chart state module ready. UI charts: slice 7.3.</p>
    </main>
  );
}
